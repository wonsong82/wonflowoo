---
name: {skill-name-with-hyphens}
description: Use when {생성이 필요한 상황/증상 - 트리거 조건만}
---

# {Generation Skill Name}

> **CRITICAL - 파일 위치**: `.claude/skills/{name}/SKILL.md` 경로에 저장 필수.
> 디렉토리명 = `name` 필드 (정확히 일치). 중첩 구조 금지.

> **CRITICAL**: Frontmatter(위 `---` 블록)는 반드시 파일 맨 처음에 raw YAML로 시작해야 합니다.
> 코드 블록(` ```yaml `)으로 감싸면 Claude Code가 인식하지 못합니다.

> **Skill Type**: Generation (분석 결과 기반 산출물 생성)

## 1. Overview

### 1.1 Purpose

{생성할 산출물과 그 목적을 명확하게 기술}

**생성 대상:**
- {산출물 1}
- {산출물 2}

**사용 용도:**
- {용도 1}
- {용도 2}

### 1.2 Generation Scope

| Aspect | Included | Excluded |
|--------|----------|----------|
| Artifacts | {생성 산출물} | {제외 항목} |
| Templates | {사용 템플릿} | {미사용 템플릿} |

---

## 2. Prerequisites

### 2.1 Input Data

```yaml
input_data:
  - name: "{입력 데이터}"
    source: "{선행 skill 출력}"
    format: "yaml"
    required_fields:
      - "{필수 필드 1}"
      - "{필수 필드 2}"
```

### 2.2 Templates

| Template | Purpose | Location |
|----------|---------|----------|
| {템플릿 이름} | {용도} | templates/{file}.yaml |

---

## 3. Methodology

### 3.1 Generation Pipeline

```
┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│  Input   │────▶│ Template │────▶│ Generate │────▶│ Validate │
│   Data   │     │  Select  │     │  Output  │     │  & Save  │
└──────────┘     └──────────┘     └──────────┘     └──────────┘
```

### 3.2 Template Selection Logic

```yaml
template_selection:
  - condition: "{조건 1}"
    template: "{템플릿 1}"
  - condition: "{조건 2}"
    template: "{템플릿 2}"
  - default: "{기본 템플릿}"
```

### 3.3 Variable Substitution

| Variable | Source | Default |
|----------|--------|---------|
| `${FEATURE_ID}` | Input data | - |
| `${DOMAIN}` | Input data | - |
| `${TIMESTAMP}` | System | Current time |

---

## 4. Outputs

### 4.1 Output Structure

```yaml
output_structure:
  base_directory: "${STAGE_OUTPUTS}/phase${CURRENT_PHASE}/"
  per_item:
    directory: "{domain}-domain/{feature-id}/"
    files:
      - "main.yaml"
      - "{sub-dir}/"
```

### 4.2 File Constraints

| Constraint | Value | Action on Exceed |
|------------|-------|------------------|
| Max lines | 300 | Split file |
| Max size | 100KB | Split file |
| Naming | {pattern} | Reject |

### 4.3 Output Template

```yaml
# Generated output template
# Generated by: ${SKILL_NAME}
# Generated: ${TIMESTAMP}

metadata:
  feature_id: "${FEATURE_ID}"
  domain: "${DOMAIN}"
  version: "1.0.0"

content:
  # 생성된 내용
```

---

## 5. Quality Checks

### 5.1 Syntax Validation

| Check | Tool | Criteria |
|-------|------|----------|
| YAML syntax | yaml-lint | No errors |
| Schema validation | json-schema | Valid against schema |

### 5.2 Content Validation

- [ ] All required sections present
- [ ] No placeholder values remaining
- [ ] References valid
- [ ] Size constraints met

### 5.3 Generation Metrics

| Metric | Target |
|--------|--------|
| Success rate | >= 95% |
| Average generation time | < 30s |

---

## 6. Error Handling

### 6.1 Generation Errors

| Error Type | Handling |
|------------|----------|
| Missing input | Skip with ERROR log |
| Template error | Use fallback template |
| Validation fail | Retry once, then ERROR |

### 6.2 Retry Policy

```yaml
retry_policy:
  max_attempts: 2
  backoff: "none"
  on_final_failure: "SKIP_AND_LOG"
```

---

## 7. Examples

### 7.1 Sample Input

```yaml
# 입력 데이터 예시
feature:
  id: "FEAT-PA-001"
  name: "생산계획 조회"
  domain: "PA"
  endpoints:
    - path: "/api/pa/plan/list"
      method: "POST"
```

### 7.2 Sample Output

```yaml
# 생성된 산출물 예시
# Generated by: discovery:spec-generation
# Generated: 2026-01-07T10:00:00Z

feature:
  id: "FEAT-PA-001"
  name: "생산계획 조회"

api_specs:
  - endpoint: "/api/pa/plan/list"
    method: "POST"
    request:
      type: "PlanListRequestDto"
    response:
      type: "PlanListResponseDto"
```
