# TODO — WonfloWoo Framework

## Pending Enhancements

### Spec Coverage Gaps
- Specs don't capture build/dev tooling (tsconfig, package.json scripts, env vars)
- HTML templates / UI markup not represented in specs
- Middleware chains not fully captured
- Cross-cutting concerns (auth, shared utils) are hard to capture in feature-grouped specs
- **Goal:** Tech leads should have 100% context from specs + arch docs alone to instruct developers without reading code
- **Investigate:** Should `_system.yml` or `conventions.yml` cover these? Or a new spec layer?

### Bootstrap Steps 6-8
- Steps 6-7 rewritten as delegated sub-agents but not re-tested on large project
- Need to verify _system.yml + architecture docs are correctly generated by sub-agents
- Re-test on the admin-service project (488 files)

### Add-Feature Test
- Run add-feature on greenfield-v3 to verify spec-updater correctly updates existing specs + _system.yml for cross-cutting changes
